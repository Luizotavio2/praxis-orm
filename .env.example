# Configuração do Banco de Dados
# Exemplo: postgresql://user:password@localhost:5432/crm
# Ou para Prisma Accelerate: prisma+https://...
DATABASE_URL=postgresql://user:password@localhost:5432/crm

# Segredo para assinatura de tokens JWT
# OBRIGATÓRIO em produção - gere uma string aleatória segura
# Exemplo: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production

# Tempo de expiração do token JWT
# Opcional - padrão: 7d (7 dias)
# Formatos aceitos: 60, "2 days", "10h", "7d"
JWT_EXPIRES_IN=7d

# Porta do servidor
# Opcional - padrão: 3000
PORT=3000

# Ambiente da aplicação
# Opcional - padrão: development
# Valores: development, production
NODE_ENV=development

# URL do frontend para configuração de CORS
# Opcional - usado apenas em produção
# Exemplo: https://meuapp.com ou http://localhost:3000
FRONTEND_URL=http://localhost:3000
